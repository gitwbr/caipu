---
description: |
  服务端上下文规则：`api_remote.js` 是当前远端服务器实际运行的后端代码。
  以后在这个项目中分析接口、数据库、存储链路、README、部署行为或服务端实现时，默认以 `api_remote.js` 为准。
  不要假设存在另一份未展示的服务端代码，也不要把数据库现状表述成已被线上实锤，除非用户明确提供了额外依据。
globs:
  - "**/*.js"
  - "**/*.md"
  - "**/*.json"
  - "**/*.wxml"
  - "**/*.wxss"
alwaysApply: true
---

- 关键约定：
  - `api_remote.js` 不是示例文件，而是远端服务器实际运行代码。
  - 讨论接口实现、表结构来源、收藏/记录/上传链路时，优先引用 `api_remote.js` 的现状。
  - 如果代码仓库中的文档与 `api_remote.js` 冲突，应先指出冲突，再以 `api_remote.js` 作为实现依据。
  - 如果用户要我整理数据库文档，我只能基于 `api_remote.js` 和仓库内材料整理，不能擅自声称那就是线上数据库真实状态。
